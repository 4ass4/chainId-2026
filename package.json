{
  "name": "chainid-2026",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy:token": "hardhat run scripts/deploy-token.js --network demochain",
    "deploy:bridge": "hardhat run scripts/deploy-bridge.js --network demochain",
    "compile": "hardhat compile"
  },
  "dependencies": {
    "ethers": "^6.9.0"
  },
  "devDependencies": {
    "hardhat": "^2.19.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0"
  }
}
