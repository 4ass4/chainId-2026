services:
  genesis-generator:
    image: hyperledger/besu:24.8.0
    container_name: genesis-gen
    user: root
    volumes:
      - .:/workspace
      - ./genesis-out:/output
    working_dir: /workspace
    command: operator generate-blockchain-config --config-file=/workspace/qbftConfigFile.json --to=/output --private-key-file-name=key
    profiles:
      - gen
